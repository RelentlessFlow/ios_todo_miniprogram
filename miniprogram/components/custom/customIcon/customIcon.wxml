<view 
  class="icon {{isCircle && 'circle'}} {{isRectCircle && 'rect-circle'}}"
  style="width: {{width}}rpx; height: {{height}}rpx;background-color: {{bgColor}};"
>
  <image 
    class="icon-img"
    src="{{src}}" 
    style="transform: scale({{scale}})"
  />
  <slot />
</view>
